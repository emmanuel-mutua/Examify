<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/flFragmentRecyclerEnrolledStudents"
    android:layout_width="match_parent"
    android:layout_margin="10dp"
    android:layout_height="wrap_content"
    tools:context=".home.admin.HomeBar.EnrolledStudents.adminViewEnrolledStudents">
    <TextView
        android:id="@+id/EnrolledStudents_textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="All Enrolled Students"
        android:gravity="center"
        android:layout_marginBottom="30dp"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="@color/black"
        />
    <androidx.recyclerview.widget.RecyclerView
        android:layout_below="@+id/EnrolledStudents_textView"
        android:id="@+id/All_EnrolledStudents_list"
        android:layout_marginTop="10dp"
        android:layout_margin="5dp"
        android:layout_width="wrap_content"
        android:layout_height="match_parent" />
</FrameLayout>